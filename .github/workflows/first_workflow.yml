name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "Hello world"
            echo "How's your day?"
      - name: Print goodbye
        run: echo "Done --- bye!"
